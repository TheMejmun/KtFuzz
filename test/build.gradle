plugins {
    alias(libs.plugins.kotlin.jvm)
    alias(libs.plugins.ksp)
}

group = 'com.samanmiran.fuzz'
version = 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':lib')
    ksp project(':lib')

    testImplementation 'org.jetbrains.kotlin:kotlin-test'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
}

test {
    useJUnitPlatform()
}
